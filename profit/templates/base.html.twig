<!DOCTYPE html>
<html lang="{{ current_language }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{{ translations.title }}{% endblock %}</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="public/assets/css/dark-theme.css">
    
    {% block styles %}{% endblock %}
</head>
<body>
    <nav class="navbar navbar-dark">
        <div class="container">
            <span class="navbar-brand mb-0 h1">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" class="bi bi-graph-up-arrow me-2" viewBox="0 0 16 16">
                    <path fill-rule="evenodd" d="M0 0h1v15h15v1H0V0Zm10 3.5a.5.5 0 0 1 .5-.5h4a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-1 0V4.9l-3.613 4.417a.5.5 0 0 1-.74.037L7.06 6.767l-3.656 5.027a.5.5 0 0 1-.808-.588l4-5.5a.5.5 0 0 1 .758-.06l2.609 2.61L13.445 4H10.5a.5.5 0 0 1-.5-.5Z"/>
                </svg>
                {{ translations.title }}
            </span>
            
            <div class="language-switcher">
                <form method="post" class="d-inline">
                    <div class="btn-group" role="group">
                        {% for lang in available_languages %}
                            <button type="submit" name="language" value="{{ lang }}" 
                                    class="btn btn-sm {% if lang == current_language %}btn-primary{% else %}btn-outline-secondary{% endif %}">
                                {{ translations.language[lang] }}
                            </button>
                        {% endfor %}
                    </div>
                </form>
            </div>
        </div>
    </nav>
    
    <main>
        {% block content %}{% endblock %}
    </main>
    
    <footer class="footer mt-5 py-3">
        <div class="container text-center">
            <small class="text-muted">{{ translations.footer.copyright }}</small>
        </div>
    </footer>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="public/assets/js/app.js"></script>
    
    {% block scripts %}{% endblock %}
</body>
</html>